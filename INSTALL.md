# FOSSLight Scanner GUI 설치 및 사용 안내

Windows에서 오픈소스 분석(소스 코드 / 의존성 / 바이너리)을 실행하고
결과를 화면에서 확인할 수 있는 데스크톱 앱입니다.

## 1. 설치 파일

배포되는 파일은 아래 **한 개**뿐입니다. 별도의 Python이나 다른 프로그램을 설치할 필요가 없습니다.

| 파일 | 크기 | 용도 |
|---|---|---|
| `fosslight-scanner-gui-0.2.1-setup.exe` | 약 259MB | 설치 프로그램 (분석 엔진 포함) |

- 지원 환경: Windows 10 / 11 (64비트)

## 2. 설치 방법

1. `fosslight-scanner-gui-0.2.1-setup.exe`를 더블 클릭합니다.
2. 파란색 **"Windows의 PC 보호"(SmartScreen)** 경고가 나타나면:
   - **추가 정보** 클릭 → **실행** 클릭
   - (코드 서명이 없는 프로그램에 표시되는 일반적인 경고입니다)
3. 설치 마법사에서 설치 위치를 확인하고 **설치**를 누릅니다.
4. 설치가 끝나면 바탕화면의 **FOSSLight Scanner** 아이콘으로 실행합니다.

## 3. 첫 분석 실행하기

1. 왼쪽 메뉴에서 **스캔 실행**을 클릭합니다.
2. **분석 대상**을 선택합니다:
   - **폴더** — 분석할 프로젝트 폴더 선택
   - **압축파일** — zip, tar.gz, jar 등 압축된 소스 선택 (자동으로 풀어서 분석)
   - **URL** — GitHub 저장소 주소 또는 압축파일 다운로드 주소 입력
3. **분석 유형**(소스 코드 / 의존성 / 바이너리)을 선택합니다. 기본은 전체 분석입니다.
4. **리포트 저장 위치**는 분석 대상을 선택하면 자동으로 채워집니다. 필요하면 변경하세요.
5. **스캔 시작**을 누르고 완료될 때까지 기다립니다.
   - 분석 대상 크기에 따라 수 분에서 수십 분까지 걸릴 수 있습니다.
   - 진행 중 로그가 화면에 표시되며, **취소** 버튼으로 중단할 수 있습니다.

## 4. 결과 확인

분석이 끝나면 왼쪽 메뉴에서 확인합니다:

- **Overview** — 검출 요약, 라이선스 위험도 통계, 보안취약점 요약
- **Scan Results > Source / Dependency / Binary** — 검출 항목 상세 (검색·정렬 가능, 행 클릭 시 상세 정보)
- **Risk > License** — 라이선스별 위험도와 준수 의무사항
- **Risk > Vulnerability** — OSS별 NVD 취약점 검색 링크

리포트 파일(엑셀 `.xlsx`, `.yaml`)은 지정한 저장 위치에도 함께 생성되므로
공유하거나 보관할 수 있습니다.

## 5. 자주 묻는 질문

**Q. 의존성(Dependency) 분석에 필요한 도구가 없다는데요?**
폴더를 분석할 때 필요한 패키지 매니저(Node.js, Maven, Python 등)가 PC에 없으면
**앱이 자동으로 설치한 뒤 분석을 진행합니다** (Windows 기본 앱 설치 관리자 winget 사용,
"도구 설치" 단계로 표시). 설치 중 Windows 권한 확인 창이 뜨면 허용해주세요.
자동 설치가 어려운 도구(Flutter 등)나 압축파일/URL 분석은 노란색 경고로 안내하며,
해당 도구를 직접 설치한 후 다시 스캔하면 됩니다.

**Q. GitHub 주소로 분석이 안 돼요.**
git 저장소 주소 분석은 PC에 [git](https://git-scm.com/download/win)이 설치되어 있어야 합니다.
압축파일 다운로드 주소(zip 등)는 git 없이도 동작합니다.

**Q. 취약점(CVE) 상세 내용이 안 보여요.**
앱은 오프라인 동작을 위해 취약점 상세를 직접 조회하지 않습니다.
Vulnerability 메뉴의 **NVD에서 검색** 버튼을 누르면 브라우저에서 상세 정보를 확인할 수 있습니다.

**Q. 스캔 로그에 경고(WARNING)가 보이는데 실패인가요?**
아닙니다. 스캔 결과 화면에 "✅ 스캔이 완료되었습니다"가 표시되고 리포트 파일이
생성되었다면 정상입니다. 오류는 빨간색 배너로 별도 표시됩니다.

## 6. 제거

Windows **설정 > 앱 > 설치된 앱**에서 `FOSSLight Scanner`를 찾아 제거하거나,
설치 폴더의 `Uninstall FOSSLight Scanner.exe`를 실행하면 됩니다.

---

문제가 발생하면 앱 왼쪽 아래의 GitHub 아이콘을 눌러 이슈로 남겨주세요.
