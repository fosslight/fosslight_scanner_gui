import { FC } from 'react';

const compare: FC = () => {
  const handleExecuteCompareCommand = (): void => {
    console.log('Execute compare command');
  };

  return (
    <div>
      <h1>Compare</h1>
    </div>
  );
};

export default compare;
